Ext.define('ManagementType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('HandleType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('City', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('UserRole', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('CustomerType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('AccidentType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('InsurancePersonLevel', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('InsuranceCategory', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});
Ext.define('RequsetLevel', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('ManagementType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('HRLevel', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('RequsetStatus', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});

Ext.define('HandleResultType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});
Ext.define('HandleLocation', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});
Ext.define('ResultsInvestigation', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});
Ext.define('DAccording', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});
Ext.define('LiftedAccording', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});
Ext.define('MeenwonType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});
Ext.define('SurveyReportType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});
Ext.define('InvoiceType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});
Ext.define('PayType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});
Ext.define('ButtonType', {
			extend : 'Ext.data.Model',
			fields : ['code', 'name']
		});